# 系统修复记录（2026-05-29）

> 全量代码扫描后的真实问题修复，分批次完成

## 修复汇总

| 轮次 | 问题 | 文件/迁移 | 状态 |
|------|------|-----------|------|
| 1 | **raw SQL参数化** (3处) | `apps/core/permissions.py` | ✅ |
| 2 | **安全HTTP头配置** | `config/settings.py` | ✅ |
| 3 | **SocialRecord.employee CASCADE→PROTECT** | `finance/models.py` → `0039` | ✅ |
| 4 | **临时文件清理** | 17个 `tmp_*.py` → `_tmp_archive/` | ✅ |
| 5 | **static/目录清理** | 非静态文件移出 | ✅ |
| 6 | **CRM CASCADE→PROTECT** (4处) | `crm/models.py` → `0028` | ✅ |
| 7 | **Finance CASCADE→PROTECT** (6处) | `finance/models.py` → `0040` | ✅ |
| 8 | **文件视图公司过滤** | `files/views.py` | ✅ |

## 验证

```
服务运行:      HTTP 200 ✅
迁移:          0039, 0028, 0040 全部已应用 ✅
Django检查:    正常工作 ✅
```

## 已验证无问题的扫描发现

- `get()` 无try/except (46处) → 全部已有保护
- `save()` 无try/except (93处) → 框架自带处理
- CSRF中间件 → 故意禁用（Token认证）
- `material/views.py`/`equipment/views.py` 公司过滤 → 已有
- `import_social_records.py` print() → 无此问题
